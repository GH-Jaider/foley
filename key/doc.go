// Package key defines the typed key model (runes, modifiers, named keys)
// shared by the tape DSL, the driver and terminal backends. Key names stay
// compatible with VHS tape names. No package may pass keys around as strings.
package key
